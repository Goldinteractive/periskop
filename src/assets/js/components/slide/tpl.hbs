{{#compare viewport.width '>=' 600 }}
	<img data-alignement="center-top" width="{{slide.big.width}}" height="{{slide.big.height}}" src="{{slide.big.url}}" />
{{else}}
	<img data-alignement="center-top" width="{{slide.small.width}}" height="{{slide.small.height}}" src="{{slide.small.url}}" />
{{/compare}}